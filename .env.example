# Environment Configuration
# Each app has its own .env file:
# - apps/api/.env - API configuration
# - apps/ui/.env - UI configuration (optional, defaults provided in docker-compose.yml)
# Copy the .env.example files in each app directory to .env and configure as needed
